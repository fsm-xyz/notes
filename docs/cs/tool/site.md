# site

使用markdown进行构建个人网站

## Nextra

基于React和Nextjs

## vitepress

+ 顶级sidebar不支持缩进，整体默认样式不好看
+ sidebar不支持自动生成

## rspress

+ md低版本1.x不支持html标签，2.0支持
+ dev模式动态更新sidebar标题，不能热重载
+ 图片路径只能放在public下面，不支持别的地方(新版本修复)
+ 不支持不带./的相对路径(新版本修复)
+ 顶级sidebar不支持缩




