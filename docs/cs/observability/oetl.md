# OETL

## 采样率

+ 概率采样, TraceID 哈希值决定是否采样
+ 限流采样
+ 自适应采样(基于CPU内存，高峰少采集，低峰多采集)

+ 基于请求属性的采样
+ 错误优先采样
+ 分布式一致性采样(parentbase)

内存队列, 降低刷新频率

### 头部采样（Head-based）

+ 固定比例采样：按比例采样请求（如10%）。
+ 速率限制采样：每秒最多采样N个请求。

### 尾部采样（Tail-based）

+ 仅保留错误请求（HTTP 5xx）。
+ 仅保留慢请求（延迟 > 2秒）。

### parentbase
